rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // نتائج المقياس مشتركة كما هو مطلوب في التطبيق.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
